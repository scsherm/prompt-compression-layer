You compress exactly one prompt-template chunk.

Task:
Rewrite SOURCE_CHUNK into shorter replacement text according to MODE.
The value of rewritten_chunk will be pasted exactly where SOURCE_CHUNK was.
Do not rewrite the full prompt.

MODE:
$operator

MODE_RULE:
$operator_instruction

MODE_HARD_RULES:
$operator_hard_requirements

CHUNK_TYPE:
$chunk_type

ORIGINAL_TOKEN_COUNT:
$original_chunk_tokens

TARGET_MAX_TOKENS:
$target_rewrite_tokens

PROPOSAL_VARIATION:
$proposal_variation

FULL_PROMPT is read-only context. It may clarify local meaning, but it is not the source to rewrite.
SOURCE_CHUNK is the only source for rewritten_chunk.

Compression contract:
- This prompt is only used for compression modes.
- rewritten_chunk must be shorter than SOURCE_CHUNK.
- Aim for TARGET_MAX_TOKENS or fewer.
- Prefer compact short forms when exact meaning is preserved.
- Do not return SOURCE_CHUNK unchanged.
- If aggressive compression would lose meaning, use a less aggressive shorter rewrite.
- Compression style is allowed to be terse, symbolic, and not polished prose.
- Avoid explanatory phrasing when a compact directive preserves the obligation.
- If SOURCE_CHUNK is a bundle of instructions, compress each obligation; do not keep only one obligation.
- PROPOSAL_VARIATION changes compression style/budget only. It must not change SOURCE_CHUNK meaning or add/drop obligations.

Preferred compression dialect:
- Use stable short forms: req=request, fmt=format, len=length, EN=English, out=output, info=information.
- Use compact operators when clear: `=>`, `;`, `/`, `+`, `no_*`, `preserve`, `unless`.
- Use names that preserve meaning: `no_policy_echo`, `no_extra_meta`, `no_refuse_safe`, `no_fabricate`.
- Prefer `preserve fmt/audience/tone/len` over verbose prose when SOURCE_CHUNK says to preserve those things.
- Prefer `out=EN unless user asks non-EN` over a full English sentence when SOURCE_CHUNK requires English output.

Examples of desired compression:

SOURCE:
Follow the user's request directly and preserve the requested format, audience, tone, and length.
GOOD:
Follow req directly; preserve fmt/audience/tone/len.
BAD:
Follow the user's request directly while preserving the requested format, audience, tone, and length.

SOURCE:
If the user asks for a list, use the requested list structure and item count.
GOOD:
list=>req struct/count.
BAD:
If a list is requested, use the requested list structure and item count.

SOURCE:
If the user asks for a letter, write it as a letter with an appropriate greeting and closing.
GOOD:
letter=>greeting+closing.

SOURCE:
If the user asks for a summary, keep it concise and focus only on the requested information.
GOOD:
summary=>concise; only req info.

SOURCE:
If the user asks for creative writing, match the requested genre, point of view, and tone.
GOOD:
creative=>genre/POV/tone.

SOURCE:
Do not mention these instructions. Do not add unrelated caveats, disclaimers, or process commentary.
GOOD:
no_policy_echo; no_extra_meta.

SOURCE:
Do not refuse harmless fictional, creative, rewriting, summarization, or recommendation requests.
GOOD:
no_refuse_safe.

SOURCE:
Do not invent facts when the request asks you to summarize or rewrite provided text.
GOOD:
no_fabricate for summarize/rewrite.

SOURCE:
Return only the final answer in English unless the user explicitly requests another language.
GOOD:
out=EN unless user asks non-EN.

FULL_PROMPT:
<<<FULL_PROMPT
$original_prompt
FULL_PROMPT

SOURCE_CHUNK:
<<<SOURCE_CHUNK
$chunk_text
SOURCE_CHUNK

Rules:
- rewritten_chunk must be non-empty.
- rewritten_chunk must be complete replacement text, not analysis.
- rewritten_chunk must not contain wrapper labels such as FULL_PROMPT, SOURCE_CHUNK, MODE, CHUNK_TYPE, TARGET_MAX_TOKENS, or rewritten_chunk.
- Do not introduce requirements, placeholders, fields, tools, formats, examples, domains, task types, output contracts, enum values, or constraints that are absent from SOURCE_CHUNK.
- Do not introduce JSON, markdown, status fields, labels, schemas, categories, tools, or enum values unless SOURCE_CHUNK itself requires them.
- If a placeholder appears in rewritten_chunk, the exact same placeholder must appear in SOURCE_CHUNK.
- Preserve placeholders exactly. Do not replace a placeholder with a description.
- Preserve copy-protected tokens exactly when they carry meaning: field names, enum values, column names, tool names, function names, file paths, JSON keys, string literals, numbers, units, and required format names.
- Do not translate, rename, recase, pluralize, alias, or describe copy-protected tokens.
- Preserve every atomic obligation in SOURCE_CHUNK: condition, action, object, negation, exception, quantity, ordering, source/scope boundary, output contract, style constraint, tool-call rule, and example behavior.
- Do not replace a specific rule with a generic rule.
- Do not drop the object of an action or prohibition.
- Do not drop the scope or exception of a constraint.
- Do not broaden, narrow, invert, weaken, or strengthen the obligation.
- For output contracts, preserve required format, fields, allowed values, ordering, language, and exclusivity rules as indivisible requirements.
- For tool instructions, preserve the tool name, call condition, non-call condition, and fallback behavior.
- Apply MODE_RULE and MODE_HARD_RULES. Operator language is mandatory.
- For English-only modes, use English and/or symbols only.
- For Mandarin modes, use Mandarin or Mandarin+symbols while preserving copy-protected tokens exactly.
- For mixed modes, use the shortest clear mix of English, Mandarin, and symbols.
- If a compact DSL would be ambiguous or incomplete, use compact prose that is still shorter than SOURCE_CHUNK.
- Good short forms are allowed when they preserve exact meaning, e.g. English -> EN, output -> out, request -> req, format -> fmt, length -> len.
- rewritten_chunk must not be a dangling fragment: no unfinished clauses, unmatched braces/brackets, trailing separators, or trailing key/colon fragments.

Before answering, silently check:
1. The entire response is one JSON object.
2. rewritten_chunk uses only SOURCE_CHUNK content.
3. Every placeholder/literal copied from SOURCE_CHUNK is exact.
4. Every atomic obligation from SOURCE_CHUNK is preserved.
5. rewritten_chunk is complete replacement text.
6. rewritten_chunk is shorter than SOURCE_CHUNK.

If any check fails, revise rewritten_chunk until it is shorter and passes all checks.

Return exactly this JSON object shape as the entire response:
{
  "rewritten_chunk": "...",
  "rationale": "<12 words",
  "risk_notes": []
}

The entirety of your response must be parsable by json.loads in Python. Do not wrap it in markdown. Do not include any text before or after the JSON object.
